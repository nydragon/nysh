import QtQuick
import "../provider"

Rectangle {
    width: parent.width
    height: width
    border.color: Qt.darker(Config.colourMain, 1.15)
    border.width: 1
    radius: 5
    color: Config.colourMain
}
